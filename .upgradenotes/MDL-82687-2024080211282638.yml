issueNumber: MDL-82687
notes:
  core_form:
    - message:
        The `duration` form field type now validates that the input value is not negative.

        This behaviour can be modified to allow negative durations by setting the `allownegative` to `true`
        (the default is `false`).
      type: improved
